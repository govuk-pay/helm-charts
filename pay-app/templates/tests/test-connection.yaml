apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pay-app.fullname" . }}-test-connection"
  labels:
    {{- include "pay-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pay-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
