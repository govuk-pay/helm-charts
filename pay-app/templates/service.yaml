apiVersion: v1
kind: Service
metadata:
  name: {{ include "pay-app.fullname" . }}
  labels:
    {{- include "pay-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pay-app.selectorLabels" . | nindent 4 }}
